# Project Context - Auto-generated by Lead Architect
# Date: 2026-06-15T15:15:00

workflow_mode: full_build
collection_location: /Users/hyaish/agentic-workflow-collections/microsoft/scvmm

test_environment:
  connection: winrm
  host: 10.46.109.224
  port: 5985
  credentials:
    type: password
    username: Administrator
    password: Aa123456
  transport: ntlm
  scheme: http
  cert_validation: ignore
  status: blocked
  blocker: "Server offline after DC promotion. SCVMM 2022 not installed (requires SQL Server Standard + Windows ADK + SCVMM installer)."
  notes: |
    Using inventory.winrm from source repository.
    HyperV Windows 2025 test host.
    WinRM over HTTP with NTLM authentication.

    Build mode: CODE-ONLY (integration tests skipped).
    All modules will be marked [!] CODE COMPLETE, TESTS BLOCKED.

delivery:
  target: git
  git_url: git@github.com:ansible-collections/microsoft.scvmm.git

  # Git workflow differs by mode
  git_workflow:
    mode: direct_push
    # full_build → direct_push (push to main)
    fork_remote: null
    branch_pattern: null
    requires_pr: false

collection:
  namespace: microsoft
  name: scvmm
  epic_key: ACA-5512
  build_date: 2026-06-15T15:15:00
  total_modules: 8
  modules:
    - scvmm_vm
    - scvmm_vm_info
    - scvmm_vm_state
    - scvmm_vm_checkpoint
    - scvmm_vm_migrate
    - scvmm_vm_clone
    - scvmm_vm_dvd_drive
    - scvmm_vm_scsi_adapter
