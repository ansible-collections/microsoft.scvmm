---
- name: Define test variables
  ansible.builtin.set_fact:
    test_baseline_name: "AnsibleTest_Compliance_{{ 99999 | random }}"

- block:
    - name: Setup - Create baseline for compliance testing
      microsoft.scvmm.scvmm_baseline:
        name: "{{ test_baseline_name }}"
        description: Baseline for compliance scan integration tests
        assignment_scopes:
          - name: "{{ test_host_group_name }}"
            type: host_group
        state: present
        vmm_server: "{{ scvmm_server }}"

    - name: Run tests
      ansible.builtin.include_tasks: tests.yml

  always:
    - name: Teardown - Remove test baseline
      microsoft.scvmm.scvmm_baseline:
        name: "{{ test_baseline_name }}"
        state: absent
        vmm_server: "{{ scvmm_server }}"
      ignore_errors: true

    - name: Teardown - Validate baseline removed
      microsoft.scvmm.scvmm_baseline_info:
        name: "{{ test_baseline_name }}"
        vmm_server: "{{ scvmm_server }}"
      register: teardown_validate

    - name: Teardown - Assert server is clean
      ansible.builtin.assert:
        that:
          - teardown_validate.baselines | length == 0
